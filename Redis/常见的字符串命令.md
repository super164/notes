1. SET name password

   该命令是用于创建键值对的命令

2. GET name:

   该命令是用于获取相应键的，键值对的值命令

3. EXISTS name:

   该命令是用于查看相应的键是否存在

4. KEYS *：

   查找所有的键

5. KEYS *me:

   表示查找所有以me结尾的键

6. FLUSHALL

   删除数据库中所有的键

7. redis-cli --raw：

   表示以原始的形式来显示内容，即可进行中文形式的输出

8. clear:

   清空屏幕

9. TTL name:

   查看键的过期时间‘

10. EXPIRE name 10:

    设置键的过期时间

    EXPIREexpire

11. SETEX name  5 你好：

    设置一个带有过期时间的键

12. SETNX name 值：

    当键不存在的时候才会设置键，如果键存在则不会进行任何动作

13. 